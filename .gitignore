# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
*.local

# Data (raw is versioned for this project; intermediates excluded)
data/processed/*
!data/processed/.gitkeep
data/external/*
!data/external/.gitkeep

# Model artifacts are versioned (< 0.5 MB total) so the API, tests, and the
# Docker stack work immediately on a fresh clone. Regenerate via `make train`.

# Outputs / caches
.pytest_cache/
.playwright-mcp/
.ipynb_checkpoints/
matplotlib/
.shap_cache/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
